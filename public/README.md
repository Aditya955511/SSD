This folder is served as static assets.

Place your .glb / .gltf models here, for example:

- /public/models/chair.glb
- /public/models/table.glb
- /public/models/sofa.glb

The app's FurnitureManager references /models/<name>.glb by default.
